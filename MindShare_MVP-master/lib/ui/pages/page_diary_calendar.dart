import 'package:flutter/cupertino.dart';

import '../../util/ms_page.dart';

class PageDiaryCalendar extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    MSPage.initRes(context);
    return Container();
  }
}